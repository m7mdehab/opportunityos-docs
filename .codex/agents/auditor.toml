name = "auditor"
description = "High-volume adjudication against stated rules. Sample review, precision measurement, classification checking."
model = "gpt-5.6-terra"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Judge each item against the written rule only. Record the matched string for
every decision. Never adjust the rule to fit the data; report the mismatch.
"""
