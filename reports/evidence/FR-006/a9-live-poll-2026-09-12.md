# FR-006 A-9 — Fresh governed live poll evidence

**Observed head:** `6493e3e4cd0321a7e61411ce3a8f3995664753cf`  
**GitHub Actions run:** `34720287787`  
**Job:** `103624788567` (`a9-live-poll`)  
**Database:** fresh PostgreSQL 16 service database `opportunityos_alpha`  
**Source:** `himalayas`  
**Registry policy at execution:** `read_allowed`

## Result

The recovery runner exercised the production `poll_source` path end to end against the live permitted Himalayas endpoint and a freshly migrated alpha database. The private Founder Truth Pack was not read. Matching used only `truth.fixtures.founder_shaped_graph()` as the synthetic engineering pack.

Observed output:

```text
A-9 live source=himalayas database=opportunityos_alpha policy=read_allowed
worker.poll_source_persisted source_id=himalayas inserted=20 unchanged=0 updated=0 evaluated_inline=20
poll_result status=ok raw=20 unique=20 inserted=20 unchanged=0 updated=0 refusal=none error=none
persist_evaluate opportunities=20 evaluations=20 fixture_rows=0 source_ids=['himalayas'] domains=['himalayas.app']
A-9 LIVE POLL PASS
```

The database was migrated from baseline through `0005_widen_location_region` before the poll. All 20 live rows were persisted and all 20 received inline match evaluations. No fixture Opportunity rows were counted as live evidence, and the run reported no policy refusal, HTTP block, or application error.

## Defect found by the first recovery attempt

The first real production-path attempt exposed a PostgreSQL `StringDataRightTruncation` on `opportunities.location_region`: a valid Himalayas native value (`Australia, Canada, Ireland, New Zealand, United Kingdom, United States`) exceeded the old `VARCHAR(64)` schema. The recovery did **not** truncate or rewrite the source value. Migration `0005_widen_location_region` widened this source-truth field to `TEXT`, and the ORM model was changed to match. The subsequent live run above passed with the original value preservable in full.

## Disposition

A-9's required live fetch → policy → parse → normalize → persist → evaluate path is freshly demonstrated on a permitted public source and a fresh alpha database. This evidence supersedes the stale recovery prerequisite that said a live A-9 run was still missing; it does not change or weaken any source-policy rule.