# Work order R6 — fail-closed source construction, breadth truth, and live poll

## Authority and objective

BRIEF-FR-006 recovery Phase E. Close fixable source-production seams, establish one
canonical board/productive-source count, then execute one controlled permitted live
poll through the real worker path on an isolated `opportunityos_alpha` database.

## Owned behavior seam

Registry policy -> adapter construction -> governed acquisition -> parse/normalize ->
persist/evaluate -> health and breadth evidence.

## Allowed files

- `opportunity/adapters/__init__.py`
- `opportunity/pipeline.py`
- direct adapter/policy tests
- `worker/handlers.py` only for a demonstrated Hacker News orchestration defect
- `worker/test_runner.py`
- bounded source-count/live-poll evidence tooling under `scripts/`
- `docs/SOURCE_REGISTRY.yaml` and `docs/SOURCE_EVIDENCE.md` only for newly observed,
  dated, policy-compliant facts
- `reports/evidence/FR-006/closure-current/a9-*`
- `reports/evidence/FR-006/closure-current/a23-*`

Do not fetch disabled/manual-only sources, retry after 403/429, bypass CAPTCHA/MFA,
use Founder credentials, broaden prepare/submit authority, count registry-only or
manual-only sources as productive reads, or claim a mock as live evidence.

## Required diagnosis and repair

1. Pass the pipeline's injected registry into adapter construction. Replace broad
   exception fallback with fail-closed handling; enumerate supported read-allowed
   bindings and explicitly classify read-allowed-but-unbound entries such as etimad.
2. Reconcile 36-new versus 48-total ATS board counts with one documented definition.
3. Verify Hacker News resolves the current Who Is Hiring thread and that every GET
   passes the registry/pacing gate. Remove or explicitly consolidate duplicate
   orchestration if safe.
4. Assess remaining board seeds offline before any resumed sweep; do not brute-force
   the known low-yield company-slug candidates.
5. Run the exact A-9 seam on fresh `opportunityos_alpha`, capturing before/after,
   source attempts, fetched/parsed/persisted/evaluated counts, provenance, IDs, health,
   and absence of synthetic residue. A legitimate zero is acceptable only after the
   seam ran.
6. Preserve Reddit as disabled/manual-only after the recorded 403 unless a currently
   governed official route exists; do not re-probe merely to satisfy the frozen row.

## Acceptance

- focused source-policy/adapter/worker tests are green.
- one controlled live-poll transcript proves the production seam ran.
- A-23 evidence prints live employment boards/300, productive new sources/8 with
  per-source row counts, HN result, Reddit disposition, and freelance manual/productive
  result. Unmet frozen targets remain `NOT_CLOSED` with exact policy/spec cause.

Commit changes and raw evidence on a dedicated branch/worktree. Report the commit.

