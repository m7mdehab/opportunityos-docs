# W2.4 Gold-set re-evaluation

## Result

The post-W2.1–W2.3 synthetic regression benchmark passed all three fixture opportunities. Expected qualification decisions, fit-score bounds, and required constraint outcomes matched for all 3/3 items. The privacy-safe sampling and label-contract tests also passed (15 tests total across both gold-set modules).

This result checks deterministic code regressions only. It is not Founder calibration: the reviewed real-job gold set must contain at least 150 Founder-reviewed opportunities, and no such label set is present in the permitted local artifacts. No Founder preference was inferred or fabricated.

## Gate

The corpus-wide backfill/calibration gate remains **closed**. The 150-real-job precision, geography, role-family, and Recommended-ordering metrics are not measured. No corpus-wide backfill was run.

## Evidence

- Focused test output: `orders/W2.4-test.txt` — 15 tests passed.
- Repository integrity: `orders/W2.4-repository.txt` — passed.
- Diff check: `orders/W2.4-diff-check.txt` — passed.
