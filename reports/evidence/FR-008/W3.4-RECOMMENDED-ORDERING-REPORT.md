# W3.4 — Recommended ordering report

**Feature status:** GREEN / REVIEWABLE / NOT FOUNDER-TESTABLE

## Implemented

- The existing `feed_projection.priority_score` now stores a deterministic base-101 lexicographic composite: eligibility tier, capability fit, preference, confidence, posting freshness, then source confidence. Each component is quantized to a 0–100 integer bucket for ranking; source values remain individually visible in the list payload.
- Missing preference, confidence, freshness, and source-confidence values use the neutral ordering bucket (50) and stay null in the API breakdown. Capability fit remains unchanged in storage.
- Existing `rank_only` filter penalties remain an outer demotion tier. ID ascending is the final tie-break after priority.
- The default feed omits proven ineligible rows, active Founder triage states/snoozes, and submitted jobs. `decision=ineligible` and `include_tracked=true` are explicit overrides; expired snoozes resurface.
- List API items now include a `ranking` object with priority, eligibility tier, capability fit, preference, confidence, posting freshness, and source confidence. No migration or backfill was added.

## Feature acceptance

- Focused storage/policy acceptance: **21 passed** (`orders/W3.4-R2-test.txt`). The initial R1 tie-break test had an incomplete synthetic fixture; its unchanged output is `orders/W3.4-R1-test.txt`, and the test-only remediation is documented in `orders/W3.4-R1-REMEDIATION-ORDER.md`.
- API suite and portable ordering payload contract: **11 passed, 19 skipped** (`orders/W3.4-R3-test.txt`). Skips are environment-dependent tests; no database connection was configured or contacted.
- Repository integrity passed (`orders/W3.4-R3-repository.txt`); diff check passed (`orders/W3.4-R3-diff.txt`).

## Limits

Ordering weights and freshness bands are deterministic interim policy, not calibrated against Founder-reviewed labels. The API list payload is covered by a database-free component contract; PostgreSQL-backed route acceptance remains pending a safe local database target. No deployment was made, and Founder-testable progress remains 0%.

## Integration

The feature checkpoint `c5f73e4` was cherry-picked onto the locked integration base `f013d357619d0c6c5c1bfc62fa586b19c6318c14`; integrated checkpoint before documentation closure: `9506e9b`.

- Focused storage/policy acceptance: **21 passed** (`orders/W3.4-INT-R1-test.txt`).
- API suite and portable ordering payload contract: **11 passed, 19 skipped** (`orders/W3.4-INT-R1-api-test.txt`). PostgreSQL-backed tests remained skipped; no database was contacted.
- Repository integrity passed (`orders/W3.4-INT-R1-repository.txt`); diff check passed (`orders/W3.4-INT-R1-diff.txt`).
- The integrated checkpoint is ready for a branch push. No merge, PR, deployment, migration, or Founder-testable claim was made.
