# W6.4 — Follow-up dates and reminders

**Status:** `INTEGRATED / PUSHED / PARTIAL / REVIEWABLE / NOT YET FOUNDER-TESTABLE`
**Feature checkpoint:** `e845f78` on `work/fr008-w64-followups`
**Integration checkpoint:** `a555806` on `work/fr008-incremental-delivery`
Both commits are pushed. This is a local review checkpoint, not terminal FR-008 acceptance.

## Delivered

- Added authenticated list/create/update API routes backed by the existing W4.1 `FounderFollowUpRecord` and `FounderActivityEventRecord`. Date-only values use UTC midnight and UTC calendar buckets; page sizes are capped at 100. No migration or schema change was added.
- Kept real follow-up changes and ID-only activity events inside the caller-owned transaction. The service covers Saved and Applied-bucket eligibility, multiple records, completion/reopen, optional private notes, idempotent replay/conflict handling, no-op behavior, and rollback.
- Added detail-drawer controls to create, reschedule, edit/clear the private note, complete, and reopen reminders. Added Tracker overview buckets for Due today, Overdue, and Upcoming with links back to the tracked job.
- Added synthetic MSW persistence in its own local-storage namespace. Tracker summary rows and activity metadata omit private note content.
- Kept the hosted adapter fail-closed with explicit 501 responses for all four follow-up routes before hosted configuration or provider access.

## Acceptance

- Temporary SQLite follow-up, tracker-note, and tracker service tests: **21 passed** on feature and integration.
- `next typegen`, `tsc --noEmit`, and ESLint: **passed** on both worktrees.
- Chromium mock tracker and keyboard coverage: **8 passed** on both worktrees. This includes all three date buckets, follow-up create/reschedule/complete/reopen, no-op edits without extra events, refresh persistence, ID-only event metadata, and note privacy in summaries.
- `python scripts/check_repository.py`: **passed** on both worktrees. Feature cached diff check and integration commit-range diff check passed.
- Feature and integration transcripts are in `orders/W6.4-FEATURE-*-test.txt`, `orders/W6.4-INT-*-test.txt`, repository/diff records, and their raw-output ZIPs.

The first feature lint attempt found an unused summary destructuring binding; the summary now selects public fields explicitly and the final zero-warning run passed. The feature worktree also needed a clean install from its committed lockfile. Initial setup failures and their remediation are documented in `orders/W6.4-FEATURE-setup-attempts.txt`; the original captured outputs and initial diff finding remain in `orders/W6.4-FEATURE-raw-output.zip`.

## Limits

The hosted adapter does not yet support atomic tracker writes, and no staging review environment is available. No live or configured database, provider, migration, private Founder data, corpus, notification, deployment, PR, or merge was used. **Founder-testable remains 0%.** W6.4 does not claim terminal FR-008 acceptance.

## Next

Continue with W6.5 after this report: implement interview tracking on the existing W4.1 substrate. Document/CV associations are a separate follow-on because there is no user-visible selectable document inventory yet. Keep the hosted boundary closed and Founder-testable at 0% until a supported atomic hosted write surface and staging environment exist.
