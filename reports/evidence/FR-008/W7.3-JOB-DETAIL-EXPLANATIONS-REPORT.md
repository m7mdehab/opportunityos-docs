# W7.3 — Job-detail explanations

State: PARTIAL / REVIEWABLE / LOCAL-ONLY
Order: orders/W7.3-JOB-DETAIL-EXPLANATIONS-ORDER.md (0cb8bbf)
Feature checkpoint: 64f3a98 (work/fr008-w73-job-detail-explanations)
Integrated checkpoint: 8dcfd9e (work/fr008-incremental-delivery)
Founder-testable: 0%

## Delivered

- Added a match overview that keeps Eligibility, Capability Fit, Preference, and Confidence distinct. It explains that Recommended is a sort order, not a complete reason for the evaluation.
- Exposed the detail API's existing preference/confidence scores and confidence-factor explanations through the TypeScript contract and detail drawer. Missing or null values show Not available; no score is replaced with zero or derived from another score.
- Kept per-dimension Capability Fit explanations, qualification constraints, strengths, gaps, and unknowns separate. Eligibility copy distinguishes unresolved evidence from an incompatible mandatory requirement, and fit weights are labelled provisional pending reviewed-label calibration.
- Expanded the local mock only with fixed synthetic scores/factors. When the no-truth-pack scenario has no evaluation, its detail response now omits stale fixture strengths, gaps, unknowns, and explanations.
- Added Chromium coverage for populated scores, no-truth-pack/unavailable evaluation, and older detail payloads where the new preference/confidence fields are absent.

## Verification

Feature and integration worktrees each passed:

- ESLint with zero warnings.
- Next route type generation and TypeScript (tsc --noEmit).
- 19 Chromium flows: 3 explanation cases plus the 12 tracker and 4 keyboard regression flows.
- python scripts/check_repository.py and git diff --check 0cb8bbf..HEAD.

OPPORTUNITYOS_DB_URL was explicitly unset for Playwright. Test transcripts, repository/diff records, HTML reports, traces, and screenshots are preserved in orders/W7.3-*-*.

The first no-truth-pack test initially served the default mock scenario because the login redirect lost the scenario query. The test helper now preserves the selected scenario through History API navigation; the final reruns passed. No scoring logic or backend API serialization changed.

## Boundaries and remaining work

No configured/live database, provider, private Founder profile/CV/truth-pack data, corpus, migration, staging, deployment, PR, merge, or Founder review was used. Fixed mock scores and rationale text are synthetic UX fixtures and are not calibrated Founder results. W7.4 desktop/mobile accessibility and Wave 8 acceptance remain. Founder-testable remains 0%; FR-008 is not accepted.
