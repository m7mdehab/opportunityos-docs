# W3.2-R3 — Geography comparison normalization

## Finding

The W3.2-R2 final acceptance is red in `test_geography_and_other_preferences_read_structured_job_attributes`. The verified synthetic preference `Egypt` and structured job country `EG` do not compare equal because the alias table returns a canonical uppercase ISO code while the raw code is normalized to lowercase. The raw R2 output remains unchanged at `W3.2-R2-test.txt`; API, repository integrity, and diff checks passed.

## Remediation

Change only the case normalization of the canonical country result in `matching/scorer.py::_normalize_geography_value`. Keep the comparison exact and structured. Do not change geographic eligibility, the test's expected score, capability aggregation, or preference scoring for other categories.

## Acceptance

1. `python -m unittest matching.test_scorer matching.test_evaluate_persist matching.test_models truth.test_predicates`
   - Expected: exit 0; country names and ISO-2 codes compare consistently, and all W3.2 focused score/persistence/predicate assertions pass.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R3-GEOGRAPHY-NORMALIZATION-test.txt`
2. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R3-GEOGRAPHY-NORMALIZATION-repository.txt`
3. `git diff --check`
   - Expected: exit 0.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R3-GEOGRAPHY-NORMALIZATION-diff-check.txt`

## Allowed files

- `matching/scorer.py` (country-code case normalization only)
- `reports/evidence/FR-008/orders/W3.2-R3-GEOGRAPHY-NORMALIZATION-*.txt` (raw acceptance output only)
- `reports/evidence/FR-008/orders/W3.2-R3-GEOGRAPHY-NORMALIZATION-READINESS.md`

All other files, data, and previous evidence are frozen. Do not access private profile data, mutate/backfill corpus rows, calibrate weights, touch shared staging or hosted services, or alter FR-007 branches.
