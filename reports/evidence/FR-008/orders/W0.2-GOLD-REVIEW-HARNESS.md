# W0.2-GOLD-REVIEW-HARNESS — Privacy-safe review contract and sampler

## Deliverable

Add a deterministic, privacy-safe data contract and stratified selection utility for the FR-008 real-job gold set. The utility accepts opaque opportunity IDs and categorical strata supplied by a later read-only corpus selection step; it must not load job descriptions, Founder data, database credentials, or hosted services. It must select unique IDs deterministically from a supplied seed, guarantee the requested per-stratum minimum or fail explicitly, and avoid committing any real opportunity IDs or Founder labels.

Add validated review labels for the brief's five actionability choices, capability-fit bands, geography/seniority/required-skill correctness, expected target-family tier, and a short reviewer rationale. Serialization must be versioned and strict; it must not accept or emit job text, Founder profile content, or unlisted fields. Do not fabricate a Founder label or implement ranking calibration in this slice.

The later real-corpus sample capture is intentionally a separate read-only order after the family/geography classification inputs are stable. This harness must support it without changing the current database or adding jobs to Git.

## Acceptance commands

1. `python -m unittest matching.test_gold_review -v`
   - Expected: exit 0; deterministic selection, uniqueness, quota enforcement, insufficient-pool rejection, accepted labels, invalid-label rejection, strict serialization, and no source content fields all pass.
   - Evidence: `reports/evidence/FR-008/orders/W0.2-gold-review-test.txt`
2. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W0.2-gold-review-repository.txt`
3. `git diff --check`
   - Expected: exit 0 with no whitespace errors.
   - Evidence: `reports/evidence/FR-008/orders/W0.2-gold-review-diff-check.txt`

## Allowed files

- `matching/gold_review.py` (new)
- `matching/test_gold_review.py` (new)
- `reports/evidence/FR-008/orders/W0.2-gold-review-*.txt` (unedited acceptance output only)

## Frozen files and actions

All other files, live project data, application state, Truth Pack and CV assets. Do not access repository `private/`, hosted databases, local profile/CV files, public job payloads, or secrets. Do not create real gold-set labels or commit real opportunity identifiers.

## Test database

`opportunityos_test_fr008_w02` (`postgresql+psycopg2://opportunityos@127.0.0.1:5432/opportunityos_test_fr008_w02`); tests are in-memory and must not connect to it.

## Worktree

Separate worktree from the authoritative FR-008 branch; do not merge or push from the implementer worktree.

## Authoritative base

`25912cd` on `work/fr008-incremental-delivery`.
