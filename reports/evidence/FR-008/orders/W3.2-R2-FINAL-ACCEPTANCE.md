# W3.2-R2 — Final preference-score acceptance

## Scope

Verify the completed W3.2 candidate after the bounded R1 synthetic-fixture correction and subsequent preference-surface refinements. This is a verification-only order: make no source or expectation changes during this run. If any row is red, preserve it and open a new named remediation order.

## Acceptance rows

1. `python -m unittest matching.test_scorer matching.test_evaluate_persist matching.test_models truth.test_predicates`
   - Expected: exit 0; verified-positive preferences compare only to structured job values; preference score remains separate from capability/qualification; serialization and persistence preserve nullable/historical behavior.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R2-test.txt`
2. `python -m unittest api.test_api`
   - Expected: exit 0; API detail returns the stored score and null for historical rows.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R2-api-test.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R2-repository.txt`
4. `git diff --check`
   - Expected: exit 0 and no whitespace errors.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R2-diff-check.txt`

## Guardrails

This order verifies only synthetic fixtures and public-code behavior. Do not access private profile data, mutate/backfill corpus rows, calibrate weights, touch shared staging or hosted services, or alter FR-007 branches. Preserve W3.2's original red log and the R1 remediation evidence unchanged.
