# W3.2-R1 — Synthetic preference fixture IDs

## Finding

The initial W3.2 acceptance is red with five errors, all at `TestPreferenceScoreIsolation._add_preference`: the fixture calls `len(graph.evidence)`, but `TruthGraph.evidence` is a lookup method. No preference assertion/scoring assertion was reached in those five cases. Preserve the full initial transcript unchanged at `W3.2-test.txt`.

## Remediation

Change only the synthetic fixture's ID generation in `matching/test_scorer.py` to use a deterministic per-test counter (or an equivalent collision-free local fixture ID). Do not change scorer behavior, expected scores, qualification fixtures, or other test fixtures.

## Acceptance

1. `python -m unittest matching.test_scorer matching.test_evaluate_persist matching.test_models truth.test_predicates`
   - Expected: exit 0; the original five fixture errors are resolved and all W3.2 score-isolation assertions pass.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R1-FIXTURE-REMEDIATION-test.txt`
2. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R1-FIXTURE-REMEDIATION-repository.txt`
3. `git diff --check`
   - Expected: exit 0.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R1-FIXTURE-REMEDIATION-diff-check.txt`

## Allowed files

- `matching/test_scorer.py` (fixture IDs only)
- `reports/evidence/FR-008/orders/W3.2-R1-FIXTURE-REMEDIATION-*.txt` (raw acceptance output only)
- `reports/evidence/FR-008/orders/W3.2-R1-FIXTURE-REMEDIATION-READINESS.md`

All other files and data are frozen for this remediation. Preserve the W3.2 initial red output. Do not access private profile data, shared staging, hosted services, FR-007 branches, or provider resources.
