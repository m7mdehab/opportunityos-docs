# W3.2-R5 — Portable preference-score API contract

## Finding

Integrated W3.2 acceptance passed, but the existing `api.test_api` suite requires `OPPORTUNITYOS_DB_URL` to name PostgreSQL. This environment has no approved local target, so 19 database-backed cases were skipped. Do not point the suite at shared or hosted storage to remove those skips.

## Remediation

Add a database-free synthetic test in `api/test_api.py` that invokes `_build_opportunity_detail` with a fake session and a synthetic persisted evaluation. Cover both a numeric `preference_score` and the historical `evaluation_detail_json=None` fallback. Keep all DB-backed API setup, production code, and route behavior unchanged.

## Acceptance

1. `python -m unittest api.test_api.PreferenceScoreDetailPortableTest`
   - Expected: exit 0, two tests passed without requiring PostgreSQL.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R5-portable-api-test.txt`
2. `python -m unittest api.test_api`
   - Expected: exit 0; database-free tests pass and PostgreSQL-only tests may skip because no approved DB target is configured.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R5-api-suite.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R5-repository.txt`
4. `git diff --check`
   - Expected: exit 0.
   - Evidence: `reports/evidence/FR-008/orders/W3.2-R5-diff-check.txt`

## Allowed files

- `api/test_api.py` (portable synthetic contract test only)
- `reports/evidence/FR-008/orders/W3.2-R5-*.txt` (raw acceptance output only)
- `reports/evidence/FR-008/orders/W3.2-R5-PORTABLE-API-CONTRACT-READINESS.md`

All other application/source/test files, work orders, evidence, and data are frozen. Do not connect to any database, access private Founder profile data, mutate corpus state, backfill or calibrate, touch shared staging or hosted services, or alter FR-007 branches.
