# W0.1-WIN-PATH — Canonical Truth Pack path portability

## Deliverable

Correct the Windows path-separator mismatch that prevents production-mode loading of the repository-managed canonical Truth Pack. Preserve the fail-closed boundary: only the exact repository snapshot may use the built-in hash and cloud-local-path exception. Add focused regression coverage for the canonical default on Windows and retain hash verification.

## Acceptance rows

| Command | Expected | Evidence file |
|---|---|---|
| `python -m unittest truth.test_pack.LoadTruthPackRemoteAndIntegrityTest.test_load_founder_pack_uses_repository_snapshot_in_cloud_mode_without_uri -v` | Exit 0; the production-mode repository-snapshot test passes on Windows and confirms a valid hash-verified load without exposing pack values. | `reports/evidence/FR-008/orders/W0.1-WIN-PATH-default-test.txt` |
| `python -m unittest truth.test_pack -v` | Exit 0; all Truth Pack tests pass. | `reports/evidence/FR-008/orders/W0.1-WIN-PATH-suite.txt` |
| `python scripts/check_guard.py --allow-missing-patterns` | Exit 0; mirror boundary passes. | `reports/evidence/FR-008/orders/W0.1-WIN-PATH-guard.txt` |
| `python scripts/check_repository.py` | Exit 0 with `Repository integrity checks passed.` | `reports/evidence/FR-008/orders/W0.1-WIN-PATH-repository.txt` |

## Allowed files

- `truth/pack.py`
- `truth/test_pack.py`
- `reports/evidence/FR-008/orders/W0.1-WIN-PATH-*.txt` (unedited raw command output only)
- Test database `opportunityos_test_fr008path` only

## Frozen files

All other tracked source, fixtures, briefs, migrations, mirror inputs, project data, and evidence. Do not access repository `private/`, CV assets, raw founder truth outside the repository-managed public-safe snapshot, or the FR-007 checkout. Do not change the schema or weaken hash/path checks.

## Test database

`postgresql+psycopg2://opportunityos@127.0.0.1:5432/opportunityos_test_fr008path`

## Worktree

`C:\Users\M7mdEhab\projects\oos-wt\fr008-w0-windows-path`

## Authoritative base

`1951dcf` on `work/fr008-incremental-delivery`.
