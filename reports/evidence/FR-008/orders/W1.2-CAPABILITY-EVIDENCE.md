# W1.2 — Capability graph and evidence normalization

## Deliverable

Normalize explicitly verified skill evidence for employment matching. The index must use a literal skill label after only Unicode normalization, whitespace collapse, and case-folding; it must not infer synonyms or read skills from narrative prose. Only verified skill-name assertions count. A proficiency tier may affect scoring only when the corresponding `skill.proficiency` assertion is verified. Include the evidence references supporting both the name and accepted proficiency. If identical normalized labels have conflicting verified proficiency tiers, keep the proficiency unknown rather than choosing an optimistic tier. Preserve canonical profile provenance and keep capability tools typed separately from career skills in employment scoring.

Add synthetic regression coverage for duplicate labels, evidence-ref aggregation, unverified proficiency, conflicting proficiency, formatting-only normalization, and non-alias behavior. Add a privacy-safe contract against the canonical hash-verified Founder Truth Pack that confirms its skill/capability graph is loaded and reference-complete without printing profile values. Do not alter the Truth Pack or CV catalog.

## Acceptance commands

1. `python -m unittest matching.test_fr008_capability_evidence -v`
   - Expected: exit 0; every scenario above passes, canonical Truth Pack loads by its pinned hash, and no profile values appear in output.
   - Evidence: `reports/evidence/FR-008/orders/W1.2-capability-test.txt`
2. `python -m unittest matching.test_scorer matching.test_skills truth.test_graph truth.test_ingest -v`
   - Expected: exit 0; existing scoring, proficiency, graph-provenance, and ingestion contracts remain green.
   - Evidence: `reports/evidence/FR-008/orders/W1.2-regression-test.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W1.2-repository.txt`
4. `git diff --check`
   - Expected: exit 0 with no whitespace errors.
   - Evidence: `reports/evidence/FR-008/orders/W1.2-diff-check.txt`

## Allowed files

- `matching/skills.py`
- `matching/scorer.py`
- `matching/test_fr008_capability_evidence.py` (new)
- `reports/evidence/FR-008/orders/W1.2-*.txt` (unedited acceptance output only)

## Frozen files and actions

All other tracked source, tests, fixtures, preferences, schemas, migrations, persistence, UI, and evidence. Do not access repository `private/`, external CV/profile files, hosted storage, or hosted databases. Do not add skill aliases, synthesize skill-to-employment relations, modify Founder facts, alter scorer weights, or print assertion values, IDs, names, dates, employers, evidence text, or Truth Pack values.

## Test database

`opportunityos_test_fr008_w12` (`postgresql+psycopg2://opportunityos@127.0.0.1:5432/opportunityos_test_fr008_w12`); acceptance tests are in-memory and must not read or mutate this database.

## Worktree

Create a separate worktree from the authoritative base before implementation. Do not merge or push from the implementer worktree.

## Authoritative base

`4043be6` on `work/fr008-incremental-delivery`.
