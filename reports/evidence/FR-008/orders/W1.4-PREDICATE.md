# W1.4 — Isolate role-title scoring from work-mode preferences

## Deliverable

Restrict the career-trajectory title-match scorer to verified `career.target_role` assertions. A verified `preference.track=remote` must not create a career-title match on a posting whose title contains “Remote”; it must not become a target-role strength or affect the title-match evidence references. Add a regression test using an isolated graph with only the typed work-mode preference, and preserve the verified target-role match behavior covered by existing tests.

## Acceptance commands

1. `python -m unittest matching.test_scorer.TestCareerTrajectoryPredicateIsolation`
   - Expected: exit 0; regression asserts no trajectory strength, no preference assertion evidence reference, and no elevated role-title-match score from `preference.track=remote`.
   - Evidence: `reports/evidence/FR-008/orders/W1.4-PREDICATE-test.txt`
2. `python -m unittest matching.test_scorer.TestTitleFamilyFitDimensionIntegration`
   - Expected: exit 0; verified target-role family match/mismatch behavior remains green.
   - Evidence: `reports/evidence/FR-008/orders/W1.4-TARGET-ROLE-test.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W1.4-repository.txt`

## Allowed files

- `matching/scorer.py`
- `matching/test_scorer.py`
- `reports/evidence/FR-008/orders/W1.4-*.txt` (raw acceptance output only)

## Frozen files

All other files. Do not access or modify repository `private/`, Founder truth/profile assets, the public mirror behavior, or any FR-007 checkout.

## Environment

- Base SHA: `38df341` (FR-008 branch after W0-MIRROR integration)
- Worktree branch: `work/fr008-w14-predicate`
- PostgreSQL test DB: `opportunityos_test_fr008pred` (`postgresql+psycopg2://opportunityos@127.0.0.1:5432/opportunityos_test_fr008pred`); tests are in-memory but the reserved DB keeps work order environments isolated.
- Web port range: not needed.


