# W2.1-R5-TEST-RECONCILIATION — Align synthetic acceptance fixtures

## Deliverable

Reconcile the two matching tests that treated legacy geography status as structured evidence. Supply explicit synthetic worldwide scope for expected remote passes and structured onsite country evidence plus verified negative authorization for the hard-negative benchmark. Reconcile the old FR-006 A-12 corpus uncertainty ceiling with FR-008 W2.1: continue reporting the aggregate, and assert that committed-corpus rows with only legacy geography labels stay review-required and never hard-fail. Do not alter production qualification code or reduce FR-008 acceptance requirements.

## Acceptance commands

1. `python -m unittest matching.test_qualification matching.test_scorer matching.test_gold_set -v`
   - Expected: exit 0; synthetic scoring and benchmark cases use structured geography evidence, while status-only metadata remains uncertain.
   - Evidence: `reports/evidence/FR-008/orders/W2.1-R5-TEST-RECONCILIATION-unit.txt`
2. `python -m unittest scripts.test_fr006_recovery.FullCorpusClusteringAcceptanceTests.test_a12_remeasures_frozen_corpus_without_inventing_missing_signals -v`
   - Expected: exit 0; aggregate uncertainty is reported, and every committed-corpus opportunity with no structured country or remote scope has a non-hard-failure uncertain geography result regardless of legacy status.
   - Evidence: `reports/evidence/FR-008/orders/W2.1-R5-TEST-RECONCILIATION-corpus.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W2.1-R5-TEST-RECONCILIATION-repository.txt`
4. `git diff --check`
   - Expected: exit 0 with no whitespace errors.
   - Evidence: `reports/evidence/FR-008/orders/W2.1-R5-TEST-RECONCILIATION-diff-check.txt`

## Allowed files

- `matching/test_scorer.py`
- `matching/test_gold_set.py`
- `scripts/test_fr006_recovery.py`
- `reports/evidence/FR-008/orders/W2.1-R5-TEST-RECONCILIATION-*.txt` (unedited acceptance output only)

## Frozen files and actions

All production source, other tests/fixtures, briefs, previous evidence, Truth Pack/profile data, migrations, databases, UI, APIs, hosted services, external sources, and user data. Do not access repository `private/` or any local CV. Do not run a full suite here; the next integrated wave owns the one full run. Preserve the first R5 raw outputs unchanged.

## Test database

None. These focused synthetic and committed-fixture tests are in-memory.

## Worktree

Create a clean isolated implementer worktree from the exact authoritative source base below. Do not push or merge from the implementer worktree.

## Authoritative source base

`9b0458f` on `work/fr008-incremental-delivery`.
