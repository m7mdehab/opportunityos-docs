# W1.3-TARGET-ROLE-TIERS — Typed target tiers and calibrated family taxonomy

## Deliverable

Add a typed, evidence-linked career target-role record with separate `career.target_role` and `career.target_role_tier` projections. Valid configured tiers are `primary`, `adjacent`, and `stretch`. A missing tier remains unknown; do not infer it from a role name, CV, capability, employment history, or normalized family. Preserve parsing compatibility for existing assertion-based target-role facts, and do not rewrite the canonical Truth Pack or any private profile data.

Expand the public, deterministic title-family taxonomy to cover the generic role examples named in BRIEF-FR-008: data integration, technical/business analyst, LLM engineering, data quality/data management, and technical consultant titles. Use synthetic unit cases only. Do not assign any role family to a Founder tier, change scoring weights/ranks, or use Founder CV content to build aliases. Gold review and Founder calibration own those decisions.

## Acceptance commands

1. `python -m unittest truth.test_models truth.test_ingest truth.test_graph truth.test_predicates matching.test_title_family matching.test_scorer -v`
   - Expected: exit 0. Typed target-role records project distinct role/tier predicates with shared stable subject IDs and preserved evidence references; legacy assertion packs continue to load; missing tier stays unknown; only the permitted role-family examples normalize to the intended known family.
   - Evidence: `reports/evidence/FR-008/orders/W1.3-focused-test.txt`
2. `python scripts/check_guard.py --allow-missing-patterns`
   - Expected: exit 0; mirror boundary passes and no private/profile material was mirrored.
   - Evidence: `reports/evidence/FR-008/orders/W1.3-guard.txt`
3. `python scripts/check_repository.py`
   - Expected: exit 0 with `Repository integrity checks passed.`
   - Evidence: `reports/evidence/FR-008/orders/W1.3-repository.txt`
4. `git diff --check`
   - Expected: exit 0 with no whitespace errors.
   - Evidence: `reports/evidence/FR-008/orders/W1.3-diff-check.txt`

## Allowed files

- `truth/models.py`
- `truth/ingest.py`
- `truth/graph.py` (only if the generic manifest projection needs a correction)
- `truth/predicates.py`
- `truth/test_models.py`
- `truth/test_ingest.py`
- `truth/test_graph.py`
- `truth/test_predicates.py`
- `matching/title_families.yaml`
- `matching/test_title_family.py`
- `matching/test_scorer.py` (only synthetic schema integration coverage)
- `docs/adr/ADR-0015-predicate-contract.md`
- `docs/templates/truth_pack.template.yaml`
- `reports/evidence/FR-008/orders/W1.3-*.txt` (unedited acceptance output only)

## Frozen files and actions

All files outside the allowed list, especially `private/`, all current Truth Pack/profile artifacts, generated mirrors, model weights, scoring weights, and previous work orders/evidence. Do not access or print private Truth Pack/CV values. Do not infer Founder target roles or tiers from employment, skills, CV names, or portfolio content. Do not modify scorer weights or set role tiers in any real profile. No database, hosted service, Supabase staging, deployment, push, or merge from the implementer worktree. If a schema conflict or migration requirement is found, stop before expanding scope and report it for a new Master work order.

## Worktree

Use a new isolated implementer worktree from the authoritative source base. Commit locally only after all listed focused acceptance checks pass. Preserve raw output unedited. Return the commit SHA, concise diff summary, and evidence paths; do not integrate directly.

## Authoritative source base

`9426822` on `work/fr008-incremental-delivery`.
