# W7.1 — To Review inbox durability and snooze parity

**State:** `PARTIAL / REVIEWABLE / LOCAL-ONLY`
**Order:** `orders/W7.1-TO-REVIEW-ORDER.md` (`4b74ec6`)
**Feature checkpoint:** `4e6578f` (`work/fr008-w71-review-inbox`), evidence cleanup `6964aea`
**Integrated source checkpoint:** `b83b68c` (`work/fr008-incremental-delivery`)
**Integrated evidence checkpoint:** `35ae16b` (`work/fr008-incremental-delivery`)
**Founder-testable:** `0%`

## Delivered

- Kept the default SQL feed behavior that excludes active tracker states and submitted jobs, retains explicit `include_tracked=true`, and resurfaces expired snoozes. Expanded temporary-SQLite coverage to the full saved, application-pipeline, rejected/closed, dismissed, and snoozed state set.
- Brought the browser mock into parity: saved/applied/pipeline/rejected/closed/dismissed jobs remain outside To Review; active and undated snoozes stay out; expired snoozes return as `To Review` without a stale Snoozed label.
- Made mock state writes a success boundary. A rejected local-storage write rolls memory back, adds no transition event, and returns HTTP 503 with an action error. The UI removes the card only after a successful mutation response.
- Added a temporary-SQLite commit-failure regression proving a failed save transaction leaves neither tracker state nor an activity event.

## Verification

Feature and integrated worktrees each passed:

- 27 focused temporary-SQLite feed-query and tracker-service tests.
- Next route type generation, TypeScript (`tsc --noEmit`), and ESLint with zero warnings.
- 13 Chromium tracker and keyboard flows, including Save/Mark Applied/Reject persistence across reload, failed-state-write rollback, and active/expired snooze behavior.
- `python scripts/check_repository.py` and working-tree plus order-base `git diff --check`.

The initial SQLite attempt found a missing test import; the first snooze browser attempt returned a non-serializable Fetch `Response` from `page.evaluate`; and one evidence wrapper ran Python from `web/`. Each issue and its correction are recorded in `orders/W7.1-FEATURE-test.txt`; all final reruns passed. Feature and integration raw logs and Chromium artifacts are preserved in the `W7.1-*-raw-output.zip` archives.

## Boundaries and remaining work

No configured/live database, PostgreSQL-backed route test, hosted provider, private Founder profile/CV data, corpus, migration, deployment, PR, merge, or Founder review was used. The local storage write-failure case is synthetic browser-mock verification; it does not substitute for hosted PostgreSQL durability proof. This checkpoint does not implement Undo; W7.2 owns reversal and history behavior. W7.3 detail explanations, W7.4 desktop/mobile accessibility, and Wave 8 acceptance remain. Founder-testable remains 0%; FR-008 is not accepted.
