# W5.3 — Feed controls and saved query views

**Status:** Integrated and pushed for review; synthetic/local verification only; Founder-testable remains 0%.

## Delivery

- Locked W5.3 order: `31b71ad` on `work/fr008-w53-feed-controls`.
- Feature source checkpoint: `70da352` on `work/fr008-w53-feed-controls`.
- Integrated source checkpoint: `5c572c7` on `work/fr008-incremental-delivery`.
- Wired the W5.1 query contract and W5.2 metadata into the feed: search, track, decision, fit minimum, six supported sorts, advanced multi-select dimensions, four independent score ranges, threshold shortcuts, posted-date bounds, count context, and active removable chips.
- Added one Sort control. The six API-supported sorts are selectable; all 53 other FR-008 §8 options remain visible in disabled groups with the W5.1 capability reason. Unsupported filter families remain visible and disabled with the W5.2 reasons, including when hosted metadata is unavailable.
- Repeated query parameters preserve OR-within-dimension and AND-across-dimensions behavior. Filter, sort, and page state is written to the URL and restored on reload and browser back/forward.
- Saved views carry a versioned envelope inside the existing `facets_json` substrate. The service returns feed query state separately, preserves legacy facet-only payload shape, and preserves an existing query when the legacy facet panel updates a view. The C1 facet panel ignores feed-query views.
- The MSW layer now supplies only synthetic metadata and deterministic synthetic query behavior. Its mock-only session marker allows the browser refresh case to exercise URL restoration without changing real authentication behavior.
- The hosted Cloudflare adapter returns an explicit 501 for advanced metadata/query capabilities. The advanced controls are disabled with the reason; the existing hosted search, track, decision, and fit-minimum path remains. Unsupported query parameters are rejected instead of silently ignored.

## Verification

- Feature branch: `python -m unittest storage.test_feed_query api.test_feed_filter_metadata api.test_feed_saved_query_views` — 24 passed using synthetic in-memory SQLite fixtures.
- Feature web checks: TypeScript (`npx tsc --noEmit`) and ESLint (`npm run lint`) passed.
- Feature mock browser: one Playwright test passed. It verifies metadata counts and scope, disabled unsupported choices, repeated multi-values, score/date/sort query state, removable chips, Clear all, saved-query round-trip, browser back/forward, and refresh restoration.
- Feature `scripts/check_repository.py` and `git diff --check` passed.
- Integration branch: the same 24 focused SQLite tests, repository integrity, and diff checks passed after cherry-pick.
- Unedited acceptance output is in `orders/W5.3-FEATURE-raw-output.zip` and `orders/W5.3-INTEGRATION-raw-output.zip`; component transcripts are beside each archive.

An early browser run exposed collapsed-group/selector assumptions and the mock store's non-persistent in-memory login on full reload. The test selectors were corrected and the mock-only local-storage session marker was added; the final browser run covers refresh and passes. No product data or credentials were involved.

## Boundaries and remaining work

No application database, live/hosted API, Supabase, migration, corpus evaluation/backfill, private Founder profile, deployment, or staging write was used. The changes are pushed review checkpoints only; no PR, merge, or Founder deployment was created. Founder-testable remains 0%.

W5.3 does not close FR-008. Real Founder gold-set calibration, subsequent tracker/Founder workflow work, PostgreSQL runtime/RLS/query-plan acceptance on an approved safe target, full staging E2E, and Founder review remain open.
