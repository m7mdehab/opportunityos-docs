# W6.2 application pipeline report

## Outcome

Feature checkpoint `5c4822e` was cherry-picked to integration as `c329627`. It adds explicit application-stage transitions on top of W6.1. `founder_triage_states` remains the only current-state row; each real stage change adds one append-only `application_stage_updated` event with its previous and next state. Repeating the current stage creates no event. The Founder can move forward through recruiter screen, assessment, interviewing, final interview, offer, and accepted, including skipping ahead. Rejected by employer, withdrawn, and no response are terminal outcomes. Backward and post-terminal transitions are rejected.

The Applied tracker bucket includes active stages and Accepted. The Rejected bucket includes the three employer/Founder terminal outcomes already represented by the tracker contract. The detail drawer shows the stage selector only while a pipeline is active, and closed states have no stage action. The mock API records synthetic events and state in separate local-storage entries so refresh and no-duplicate-event behavior are testable.

Stage transitions do not write outbound application actions. W6.1 Mark Applied continues to produce the single Founder-attested action. The hosted adapter retains its 501 response until an atomic state-plus-event operation exists.

## Verification

- `python -m unittest api.test_tracker_service`: final R3 run passed 10 temporary-SQLite tests. Coverage includes every forward stage, all terminal outcomes, skipped stages, append-only events, idempotent replay, stale-key response state, invalid backward/reopen transitions, tracker buckets, and no extra outbound action for stage changes.
- `npx next typegen`: passed.
- `npx tsc --noEmit`: passed.
- `npm run lint`: passed.
- `npm run test:e2e -- tests/e2e/fr008-tracker-triage.spec.ts tests/e2e/keyboard.spec.ts`: 6 Chromium tests passed. The pipeline flow covers skipping to Assessment, repeating the current stage without another event, advancing to Offer and Accepted, and recording Rejected by employer in the Rejected bucket with stage actions closed.
- `python scripts/check_repository.py`: passed.
- `git diff --check`: passed; staged diff check is recorded before commit.
- Integration rerun: 10 temporary-SQLite tests, TypeScript, ESLint, all 6 Chromium tests, repository integrity, and the integration diff check passed. The `W6.2-INT-*` outputs are preserved beside the feature outputs.
- The initial service run R1 revealed that the shared transition validator treated `applied` as a pipeline-only target even for W6.1 Mark Applied. The validator was scoped to `set_stage`; R2 and R3 passed. The initial error output is retained unchanged.

## Boundaries and remaining work

This is a local API and synthetic mock checkpoint. No database/provider, migration, real corpus, private Founder data, application-data write, deployment, or Founder review was used. The hosted tracker API remains 501; Founder-testable remains 0%.

Restore, application metadata, notes, follow-ups, contacts, interview records, CV/document associations, timeline UI, advanced tracker filters/sorts, and full immutable snapshot retention remain future slices. PostgreSQL runtime/RLS/query-plan acceptance and staging E2E remain pending a safe, atomic hosted write surface. This checkpoint does not claim FR-008 terminal acceptance.

## Evidence files

Raw feature and integration outputs are preserved under `reports/evidence/FR-008/orders/` with the `W6.2-FEATURE-` and `W6.2-INT-` prefixes.
