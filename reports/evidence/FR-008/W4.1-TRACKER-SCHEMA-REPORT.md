# W4.1 — Founder tracker schema foundation

**Status:** `PARTIAL / REVIEWABLE / SCHEMA ONLY`

## Implemented

Added additive migration `0015_fr008_tracker_foundation` and matching ORM models. The migration adds nullable `saved_at`, `applied_at`, and `closed_at` fields to the existing triage row without changing old values or inferring timestamps. It adds an append-only activity event table with unique optional idempotency keys, plus normalized application details, private notes, follow-ups, interview records, document links, and compact tracker snapshot metadata.

Snapshot metadata stores a small title/location/source/score/version record and references an `artifact_cache` cold object for content. It does not add descriptions or CV content to a new hot table. The tracked metadata uses restrictive opportunity/artifact foreign keys to preserve referenced records. PostgreSQL migration SQL enables RLS and revokes browser access for new private tables; an activity trigger blocks event updates/deletes. The existing `dismissed`/`snoozed` values remain valid; no transition API or state check constraint was added.

## Acceptance

- Local SQLite schema/model tests: **5 passed** (`orders/W4.1-R1-test.txt`).
- Offline PostgreSQL Alembic upgrade and downgrade SQL rendered successfully. The exact raw R1 renderer outputs and diff-check diagnostics are preserved in `orders/W4.1-R1-raw-output.zip`; archive entry hashes are recorded in the remediation order. The URL was a dummy loopback target and no database connection or migration application occurred. Alembic's generated trailing whitespace made the first staged diff check red; the raw output was preserved unchanged and excluded from the final text diff via the archive.
- Repository integrity passed and the final staged diff check passed (`orders/W4.1-R4-repository.txt`, `orders/W4.1-R4-diff.txt`).

## Limits and next work

This is schema only: tracker actions, state-transition validation, event writes, snapshot capture, notes/reminder APIs, and Founder UI remain for later slices. PostgreSQL runtime/RLS behavior and query plans remain unverified because no safe local database target is configured. Contacts remain omitted as optional. Do not roll back after tracker rows have been written without first preserving those rows.

## Integration acceptance

The W4.1 feature schema checkpoint `ed76f38` was cherry-picked onto the locked integration branch after its work-order commit.

- SQLite tracker-schema tests: **5 passed** (`orders/W4.1-INT-R1-test.txt`).
- Offline PostgreSQL-targeted Alembic upgrade and downgrade SQL both rendered successfully. Exact raw outputs are archived in `orders/W4.1-INT-R1-raw-output.zip`; no database was contacted and no migration was applied.
- Repository integrity and diff checks passed (`orders/W4.1-INT-R1-repository.txt`, `orders/W4.1-INT-R1-diff.txt`).
- Feature-branch R1 diff diagnostics caused by Alembic renderer whitespace are retained byte-for-byte in `orders/W4.1-R1-raw-output.zip`, with SHA-256 verification recorded in the remediation order.
