# W7.2 — One-click triage and Undo

**State:** `PARTIAL / REVIEWABLE / LOCAL-ONLY`
**Order:** `orders/W7.2-ONE-CLICK-UNDO-ORDER.md` (`48d14ac`)
**Feature source checkpoint:** `74c8127` (`work/fr008-w72-one-click-undo`)
**Feature acceptance-test checkpoint:** `23a25ef`
**Integrated source checkpoint:** `230c15a` (`work/fr008-incremental-delivery`)
**Integrated acceptance-test checkpoint:** `3235eb3`
**Founder-testable:** `0%`

## Delivered

- Added card-level Save, Mark Applied, and Reject actions while keeping the detail-open action separate. Successful card and drawer actions offer a short-lived Undo prompt without exposing internal event IDs or idempotency keys.
- Added authenticated restore handling for the latest eligible Save/Mark Applied/Reject event. Restore checks current state and intervening activity, returns 409 for stale or unrelated events, restores the exact prior state snapshot (including snooze expiry), appends one reversal event transactionally, and returns the same reversal on an idempotent retry.
- Undo of Mark Applied preserves the original attestation row for audit with status `undone`; feed selection no longer suppresses a job solely because of that undone attestation.
- Brought the synthetic browser API and local-storage tracker into parity for quick actions, Undo, stale conflicts, retries, timeline history, reload persistence, and storage-write failure rollback. A failed write retains the prior state and creates no transition event.
- Kept the hosted restore route as a static 501 response before hosted configuration, authentication, or provider work. No schema migration was needed.

## Verification

Feature and integration worktrees each passed:

- 33 focused temporary-SQLite tracker/feed tests, including restoration to To Review and Saved, preservation of `saved_at` and snooze expiry, `undone` attestation status, append-only reversal event, idempotent retry, stale and unrelated-event conflicts, and commit rollback.
- Next route type generation, TypeScript (`tsc --noEmit`), and ESLint with zero warnings.
- 16 Chromium tracker and keyboard flows covering card and drawer Undo, queue/tracker placement after reload, applied audit history, stale Undo, failed mock storage writes, and existing tracker workflows.
- `python scripts/check_repository.py` and `git diff --check 48d14ac..HEAD`.

`OPPORTUNITYOS_DB_URL` was explicitly unset for Playwright. Paired test transcripts, repository/diff records, Playwright reports, and test-result artifacts are preserved under `orders/W7.2-*-*`.

The feature run also retained diagnostic remediation notes: invalid stale-transition fixture, generated Next route types required before direct `tsc`, an ambiguous alert locator, detail-data reset during state synchronization, a query-string-tolerant login URL assertion, and a drawer-local Undo notice needed while the detail sheet is open. The final reruns passed on both branches.

## Boundaries and remaining work

No configured/live database, PostgreSQL runtime, provider, private Founder profile/CV data, corpus, migration, staging, deployment, PR, merge, or Founder review was used. The local storage failure case is synthetic browser-mock verification and does not establish hosted durability. W7.3 job-detail explanations, W7.4 desktop/mobile accessibility, and Wave 8 acceptance remain. Founder-testable remains 0%; FR-008 is not accepted.
