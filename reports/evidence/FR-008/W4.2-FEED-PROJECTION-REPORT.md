# W4.2 feed projection population report

**State:** `PARTIAL / REVIEWABLE / NOT YET FOUNDER-TESTABLE`

**Feature branch:** `work/fr008-w42-projection-fields`
**Locked integration base:** `ee5b9d5`

**Integration branch:** `work/fr008-incremental-delivery`

**Integrated checkpoint:** `0a13113` (after locked order `95c2f05`)

## What changed

The projection model and additive migration `0016_fr008_projection_fields` add nullable title level, target tier, preference score, and confidence score columns. Newly built projection rows normalize family and level from the posting title, resolve target tier only from verified role and same-subject tier evidence, and persist preference/confidence separately from capability fit and Recommended priority. The projection version is now `v2`.

`outside_targets` is emitted only for a recognized title family when the supplied TruthGraph contains verified positive target roles but none normalize to that family. Unknown title families, no target-role evidence, missing tiers, and conflicting tiers remain `NULL`. Preference and confidence are stored only when finite numeric values fall in the inclusive 0–100 range. The same validated values feed the existing priority calculation; W3.4 ordering and weights are unchanged.

The migration is additive and reversible. Existing projection rows are not rewritten, so their new columns stay `NULL` until a later refresh. This slice adds no feed API filters or Founder UI behavior.

## Evidence

- Focused projection, title normalization, and scorer acceptance: 137 tests passed, no failures/errors. The final output is in `orders/W4.2-R4-test.txt` and its byte-identical archive is `orders/W4.2-R4-tests-raw-output.zip`; initial and earlier test outputs are preserved in `orders/W4.2-R1-tests-raw-output.zip`.
- Repository integrity: passed (`python scripts/check_repository.py`); complete output is preserved in `orders/W4.2-R4-repository-raw-output.zip`.
- Alembic upgrade and downgrade SQL rendered offline against the dummy loopback URL `postgresql+psycopg2://offline_user@127.0.0.1:1/never_connect`; both returned exit code 0. Renderer stdout and stderr are archived byte-for-byte in `orders/W4.2-R1-raw-output.zip`. No database was contacted or migrated.
- The final staged `git diff --cached --check` after both documentation remediations exited 0 with no output (`orders/W4.2-R4-diff.txt`). The Markdown trailing-space result and the follow-up artifact-handling result are preserved byte-for-byte in `orders/W4.2-R2-diff-raw-output.zip` and `orders/W4.2-R3-diff-raw-output.zip`, with remediations in the matching order files.
- Initial focused R1 output remains unchanged: 17 tests with one freshness-expectation failure and four synthetic TruthGraph fixture errors. `W4.2-R1-REMEDIATION-ORDER.md` records the test-only fixes; the original output is preserved in the raw-output archive.

## Boundary and next step

No existing opportunity or evaluation corpus records were modified; only synthetic temporary SQLite fixtures were used. No corpus backfill, evaluation rerun, migration application, staging deployment, or Founder-private data access occurred. Founder-testable remains 0% because these fields are not exposed in the Founder interface.

## Integrated acceptance

The feature checkpoint was cherry-picked onto `work/fr008-incremental-delivery` as `0a13113`. Integration repeated the 137 focused projection/title-normalization/scorer tests successfully, repository integrity passed, and `git diff --check ee5b9d5..HEAD` exited 0. Alembic upgrade and downgrade SQL rendered offline with the dummy loopback URL and both returned exit code 0; renderer output is preserved in `orders/W4.2-INT-R1-raw-output.zip`. Integration test, repository, and diff outputs are preserved in `orders/W4.2-INT-R1-checks-raw-output.zip`. No database was connected or migrated. This checkpoint has been pushed; projection fields remain unexposed, so it is reviewable code and not Founder-testable.
