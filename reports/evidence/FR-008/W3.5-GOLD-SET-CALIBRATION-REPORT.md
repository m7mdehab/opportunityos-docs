# W3.5 — Gold-set calibration evaluator

**Status:** `PARTIAL / REVIEWABLE / CALIBRATION PENDING`

## Implemented

Added `matching/ranking_calibration.py`, a deterministic offline evaluator for typed W0.2 gold-review labels and caller-supplied predictions. It computes hard-ineligible precision and false-positive/false-negative counts, reviewer-marked geography/seniority/skill correctness, target-tier accuracy, Recommended apply/likely-versus-probably-skip pairwise concordance, capability-fit Spearman rank correlation, and mean fit scores by Founder fit band. It reports the brief's 100%, 95%, and 90% targets and keeps unavailable measures null with a limitation.

Reports contain aggregate counts and stable one-way hashed review tokens for eligibility false positives/negatives. They omit raw job IDs, reviewer rationales, job text, and Founder/profile content. The 150-item count alone does not attest a real Founder-reviewed dataset; the caller must explicitly attest that provenance before `minimum_sample_met` can become true.

No production scoring or ranking parameter was tuned. The evaluator does not load jobs, profile material, a database, or the network.

## Acceptance

- Synthetic evaluator tests: **7 passed** (`orders/W3.5-R2-test.txt`). R1 also passed the initial suite (`orders/W3.5-R1-test.txt`).
- Repository integrity passed (`orders/W3.5-R2-repository.txt`); diff check passed (`orders/W3.5-R2-diff.txt`).
- Tests cover eligibility precision and hashed FP/FN tokens, review metrics, target tiers, ordering pairs and ID tie-break, fit rank correlation, unavailable metrics, input integrity, and the 150-item count/provenance distinction.

## Calibration gate

The approved Founder-reviewed set of at least 150 real jobs was not supplied in the authorized workspace. Consequently no real calibration metrics, independent false-positive/false-negative review, score-weight change, corpus backfill, or terminal calibration claim is made. This evaluator makes that later review reproducible; it does not substitute synthetic labels for Founder judgment.

## Integration acceptance

Feature checkpoint `4f75f5f` was cherry-picked onto integration checkpoint `a686bce` as `dedf501`.

- Gold review contract, synthetic benchmark, and calibration evaluator: **22 passed** (`orders/W3.5-INT-R1-test.txt`).
- Repository integrity passed (`orders/W3.5-INT-R1-repository.txt`); diff check passed (`orders/W3.5-INT-R1-diff.txt`).
- No database or private profile material was accessed. The integration checkpoint is reviewable, while actual calibration remains pending.
