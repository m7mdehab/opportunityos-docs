# W7.4 — Desktop/mobile accessibility and keyboard behavior

State: PARTIAL / REVIEWABLE / LOCAL-ONLY
Order: `orders/W7.4-DESKTOP-MOBILE-ACCESSIBILITY-ORDER.md` (`8c6cba3`) and tracker file correction (`de84515`)
Feature checkpoint: `0d9bcb3` (`work/fr008-w74-mobile-accessibility`)
Integrated checkpoint: `202b3bc` (`work/fr008-incremental-delivery`)
Founder-testable: 0%

## Delivered

- Capped the feed Track, Decision, and Sort controls at a usable width. The browser had sized the Sort select to its longest disabled option, expanding the mobile page to 1087px; the feed now has no page-level horizontal overflow at 390px.
- Marked the Jobs/Tracker switch as a named navigation landmark, resolving the feed axe landmark violation.
- Restored focus to the external “More filters” trigger after the advanced filter sheet closes. Detail-sheet focus return and Escape behavior are covered as well.
- Added `web/tests/e2e/fr008-responsive-accessibility.spec.ts`. At 1280px and 390px it covers feed/filter/detail/tracker overflow and axe checks, filter and sort selection, saved-query restore, hidden To Review triage, Save/Applied/Rejected bucket placement, Undo/restore, stage updates, follow-up creation, and refreshed activity history. It also verifies that text input, button, link, and overlay focus do not trigger triage shortcuts.

## Verification

Both feature and integration worktrees passed:

- ESLint with zero warnings.
- Next route type generation and `tsc --noEmit`.
- 21 Chromium tests: responsive A-27 desktop/mobile journeys, login/feed and overlay axe scans, saved-query/feed controls, tracker flows, activity history, and keyboard shortcuts.
- `python scripts/check_repository.py` and commit-range `git diff --check`.

`OPPORTUNITYOS_DB_URL` was explicitly unset for Playwright; tests used the local synthetic mock. Branch transcripts and repository/diff records are in `orders/W7.4-FEATURE-*` and `orders/W7.4-INT-*`; the raw-output archive groups them.

## Remediation notes

- Turbopack rejected a temporary `node_modules` junction that pointed outside the feature worktree. The junction was removed and a local `npm ci` completed; no lockfile change was made.
- Initial scans found the unlabelled landmark, mobile Sort intrinsic-width overflow, and missing filter-sheet focus return. The named navigation landmark, width cap, and trigger refocus were added and passed both viewport scans.
- The mock seed has one employment job eligible for quick triage. The A-27 test cycles that synthetic job through Save/Undo, Reject/Undo, then Apply, checking each tracker bucket before restoring it. This validates the UI journey without inventing additional mock jobs.
- Clearing the query writes `sort_by=recommended` explicitly, so the test asserts the normalized URL value. No production behavior was changed to accommodate a test assumption.

## Boundaries and remaining work

No configured or live database, provider, private Founder profile/CV/truth-pack data, corpus or backfill, schema or migration, staging, deployment, PR, merge, or Founder review was used. W7.4 is local mock evidence only; hosted Tracker operations that return 501 are not persistence proof. Wave 8 gold-set validation and any Founder review remain gated on an authorized reviewed gold set and a safe available review environment. FR-008 is not terminal PASS; Founder-testable remains 0%.
