# W6.5 — Interview tracking

**Status:** `INTEGRATED / PUSHED / PARTIAL / REVIEWABLE / NOT YET FOUNDER-TESTABLE`
**Feature checkpoint:** `a1cb466` on `work/fr008-w65-interviews`
**Integration checkpoint:** `179bd14` on `work/fr008-incremental-delivery` before this report/evidence commit
**Order lock:** `98168f1`

This is a local code and evidence checkpoint, not terminal FR-008 acceptance.

## Delivered

- Added authenticated, bounded interview list/create/update API operations and an upcoming Tracker summary. The service reuses the W4.1 `FounderInterviewRecord` and `FounderActivityEventRecord`; no migration or schema change was added.
- Enforced the Applied-bucket allowlist, offset-aware datetime input normalized to UTC, page-size cap, enumerated type/format/outcome values, and trimmed text limits. Per-job responses include private preparation/debrief notes. Upcoming rows exclude those fields.
- Persisted each real interview mutation and its append-only event inside the caller-owned SQL transaction. Create/update/completion events carry only `interview_id` metadata; retries are idempotent, conflicting key reuse fails, and no-op edits/outcomes add no event.
- Added interview creation, edit, outcome/completion, loading, empty, and error states in the job detail drawer, plus UTC-labeled upcoming interview rows with links back to each applied job in Tracker.
- Added synthetic mock persistence in the dedicated `opportunityos.mock.interviews.<scenario>` namespace. Mock event metadata is ID-only and summary rows omit private notes.
- Kept the hosted adapter fail-closed with explicit 501 responses for all four interview route shapes before hosted configuration, authentication, or provider work.

## Acceptance

- Temporary in-memory SQLite interview, follow-up, note, and tracker service tests: **25 passed** on feature and integration.
- `next typegen`, TypeScript, and ESLint: **passed** on feature and integration; ESLint reported zero warnings.
- Chromium mock tracker and keyboard coverage: **9 passed** on feature and integration. The interview flow covers scheduling, creation, edit, no-op event behavior, completion, refresh persistence, ID-only event metadata, and private-note exclusion from the upcoming summary.
- `python scripts/check_repository.py` and feature/integration diff checks: **passed**.
- Feature transcripts and remediation evidence are preserved in `orders/W6.5-FEATURE-test.txt` and `orders/W6.5-FEATURE-raw-output.zip`. Integration transcripts are preserved in `orders/W6.5-INT-test.txt` and `orders/W6.5-INT-raw-output.zip`.

Initial fixture, lint, type, environment setup, and mock eligibility failures were corrected and recorded with the final green runs in the feature raw-output archive. No live/configured database, hosted provider, private Founder content, migration, corpus, deployment, PR, merge, or Founder review was used. Founder-testable remains **0%**.

## Limits and next

PostgreSQL-backed route execution and hosted atomic writes remain unverified; hosted interview routes return 501. Document/CV association remains a separate increment because no selectable document identity inventory exists in the current application surface. W6.5 does not add contacts, document selection/linking, timeline/history UI, restore, or snapshot retention.

Proceed with the separately ordered W6.6 document/CV association slice. Its contract must define selection from document IDs only and keep document contents out of interview/tracker summaries. Continue to keep Founder-testable at 0% and do not claim terminal FR-008 acceptance.
